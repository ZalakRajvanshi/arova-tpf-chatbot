# Local dev: leave VITE_API_URL unset — Vite proxy will forward /api calls to localhost:8000
# Production (Vercel): set this to your Render backend URL, e.g.:
VITE_API_URL=https://tpf-backend-5mei.onrender.com
